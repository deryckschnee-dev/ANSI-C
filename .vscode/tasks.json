{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Clang - build_exe.bat",
			"command": "${workspaceRoot}\\bin\\build_exe.bat ${fileBasenameNoExtension}",
			"options": {
				"cwd": "${workspaceRoot}\\bin"
			},
			"detail": "Build active sourcefile and generate executable.",
		},		
		{
			"type": "shell",
			"label": "Clang - clean.bat",
			"command": "${workspaceRoot}\\bin\\clean.bat",
			"options": {
				"cwd": "${workspaceRoot}\\bin"
			},
			"detail": "Removes all temporary build files in \\bin directory."
		},
	]
}